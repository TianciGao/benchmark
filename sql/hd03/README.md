# HD-03 SQL Scaffolding

These SQL files are local scaffolding for `HD-03` command-slot concretization only.

They do not execute a real `TPC-H` or `TPC-DS` pilot by themselves and they do not imply any final `HD-03` decision.

Current files:

- `load_anchor_tpch.sql`
- `load_anchor_tpcds.sql`
- `pilot_queries_tpch.sql`
- `pilot_queries_tpcds.sql`

Current status:

- the load files are placeholder no-op stubs that record expected variables
- the pilot query files are placeholder no-op stubs that record expected benchmark identity
- later work may replace these stubs with real pilot SQL only under explicit direction
