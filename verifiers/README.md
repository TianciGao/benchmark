# Verifiers

This directory is reserved for `V0` to `V3` verifier adapters.

Phase 1 status:

- No full verifier adapters are implemented yet.
- The smoke path performs only a minimal byte-level stdout comparison for a deterministic scalar query.

TODO:

- Add `V0` parse/executability
- Add `V1` differential execution
- Add `V2` plan-aware generated tests
- Add `V3` formal proving adapters
