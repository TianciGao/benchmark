# PostgreSQL Engine Skeleton

This directory reserves the PostgreSQL engine setup assets for the core evaluation.

Phase 1 status:

- Runner skeleton lives in `scripts/postgres_runner.py`
- Exact PostgreSQL version and configuration are still blocked on `HD-02`
- Exact reference hardware profile is still blocked on `HD-01`

TODO:

- Add pinned container image and configuration bundle after human approval
- Add engine bootstrap scripts after Phase 1 environment freeze
