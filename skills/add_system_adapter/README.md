# Skill: add_system_adapter

Purpose:

- Add a new system adapter under `systems/`
- Implement the frozen adapter interface
- Add a smoke test and metadata export

Expected inputs:

- System family
- Upstream repo or source reference
- Target adapter location

Expected outputs:

- Adapter module
- Smoke test path
- Metadata manifest

TODO:

- Expand into a full Codex skill once Phase 3 begins
