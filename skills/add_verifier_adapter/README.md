# Skill: add_verifier_adapter

Purpose:

- Add a verifier adapter under `verifiers/`
- Implement the frozen `verify(...)` and evidence-trace interface

Expected inputs:

- Verifier stage (`V0`, `V1`, `V2`, or `V3`)
- Interface contract
- Test fixture

Expected outputs:

- Verifier adapter module
- Evidence trace schema
- Smoke test

TODO:

- Expand into a full Codex skill once Phase 4 begins
