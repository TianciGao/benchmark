# Anchor Pack

This directory will eventually hold the official `anchor` pack manifests.

Current status:

- `tiny_smoke.yaml` is a bootstrap-only fixture for Phase 1.
- It is not the official frozen `anchor` workload.
- Official TPC-H, TPC-DS, and JOB membership remains a Phase 2 task and still depends on human decisions.
