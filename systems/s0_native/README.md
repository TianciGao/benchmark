# S0 Native Adapter

This directory holds the `S0` baseline adapter.

- Family: `S0`
- Behavior: native PostgreSQL optimizer, no SQL rewrite
- Phase 1 status: minimal smoke-path implementation is available in `adapter.py`
- TODO: add richer metadata export and integration tests once the reference engine profile is frozen
